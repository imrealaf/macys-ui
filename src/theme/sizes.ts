export enum HeaderHeight {
  default = 64,
  sm = 56,
}

export enum LogoHeight {
  default = HeaderHeight.default - 20,
  sm = HeaderHeight.sm - 20,
}

export enum DrawerWidth {
  default = 240,
  md = 200,
}
