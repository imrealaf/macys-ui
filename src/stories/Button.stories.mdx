import { action } from '@storybook/addon-actions'
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { Button } from '@mui/material'

<Meta
  title='Buttons'
  component={Button}
  argTypes={{
    size: {
      name: 'Size',
      options: ['small', 'medium', 'large'],
      control: {
        type: 'inline-radio'
      }
    },
    disabled: {
      name: 'Disabled',
      defaultValue: false,
      control: {
        type: 'boolean'
      }
    }
  }}
/>

export const Primary = (args) => <Button {...args} children={'Primary'} />
export const Secondary = (args) => (
  <Button color='secondary' {...args} children={'Secondary'} />
)

# Examples

<Canvas>
  <Story name='Primary'>{Primary.bind({})}</Story>
  <Story name='Secondary'>{Secondary.bind({})}</Story>
</Canvas>

# Props

<ArgsTable of={Button} />
